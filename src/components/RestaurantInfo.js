// 1) Un composant RestaurantInfos => Afficher les infos du restaurant
// 1.a) L'adresse va dans un nouveau composantégalement

// 2) Créer le composant Playlist => Affiche la liste des Playlist

// 3) Créer le composant PlaylistItem => Afficher un item de la iste des plats

function RestaurantInfo () {
  return (
    <h1>RestaurantInfo</h1>
  )
}

export default RestaurantInfo
